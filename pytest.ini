[pytest]
junit_family = xunit1
testpaths = rapidtide/tests
pythonpath = .
addopts = --strict-markers --strict-config
markers =
    unit: fast unit-level tests (parser, pure math, small mocked checks)
    slow: heavier workflow-oriented tests
